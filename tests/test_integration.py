import pytest
from lib.food import Food
from lib.restaurant import Restaurant
from lib.customer import Customer

# Test that on init, view customer basket returns an empty list

# Test that list restaurant food returns a list of available food instances

# Test that list_ffod on an empty restaurant returns an error

# Test that add item, adds item to user basket